rootProject.libraryModulePlugins.each {
    apply plugin: it
}

// Commented out temporarily - needs migration to maven-publish plugin
// apply from: 'maven-push.gradle'

android {
    def ext = rootProject.extensions.ext
    namespace 'com.michaelsiddi.library'
    compileSdkVersion ext.android.compileSdkVersion
    defaultConfig {
        minSdkVersion ext.android.minSdkVersion
        targetSdkVersion ext.android.targetSdkVersion
        testInstrumentationRunner "androidx.test.runner.AndroidJUnitRunner"

    }
    // dataBinding {
    //     enabled = true
    // }
    defaultConfig {
        multiDexEnabled true
    }
    compileOptions {
        sourceCompatibility JavaVersion.VERSION_17
        targetCompatibility JavaVersion.VERSION_17
    }
    kotlinOptions {
        jvmTarget = '17'
    }
}

// Commented out - needs migration to new Dokka plugin API
// dokka {
//     outputFormat = 'html'
//     outputDirectory = "$buildDir/javadoc"
// }

dependencies {
    rootProject.libraryModuleDependencies.each {
        add(it.configuration, it.dependency, it.options)
    }
}
repositories {
    mavenCentral()
}

// Commented out temporarily - deprecated plugin
// apply plugin: 'com.github.dcendents.android-maven'